<div className="background">
            <div>{/* Green Background */}</div>
            <h1>{homeContent.title}</h1>
            <h3>{homeContent.subtitle}</h3>
            <p>{homeContent.subcontent}</p>
            <button>{homeContent.buttonTitle}</button>
          </div>
          <div>
            <div>
              <div>{/* Green Background */}</div>
              <h1>{homeContent.title}</h1>
              <h3>{homeContent.subtitle}</h3>
              <p>{homeContent.subcontent}</p>
              <button>{homeContent.buttonTitle}</button>
            </div>
            <div>
              {/* Food Picture */}
              <img src="" alt="" />
            </div>
          </div>

          <div>
            <div>
              <h1>{homeContent.thisWeekTitle}</h1>
              <button>{homeContent.thisWeekButton}</button>
            </div>
            <div>
              {specials.map((item) => {
                return (
                  <Card
                    title={item.title}
                    price={item.price}
                    imgUrl={item.imgUrl}
                  />
                );
              })}
            </div>
          </div>